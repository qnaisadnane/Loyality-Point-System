{% extends 'layouts/base.twig' %}

{% block content %}
<div class="dashboard">
    <h2>Bienvenue {{ user.name }} !</h2>

    <div class="stat-card">
        <h3>Vos points</h3>
        <p class="points">{{ user.total_points }} {{ session['user_id']}}</p>
    </div>
</div>
{% endblock %}
